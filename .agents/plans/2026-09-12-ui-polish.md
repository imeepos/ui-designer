# 2026-09-12 核心页面持续打磨计划（v1）

## 触发器
用户直接指令「持续打磨核心页面」（2026-09-12 07:31）。前序计划 cms-integration v2 已收官（见 2026-09-11-cms-integration.md）。

## 范围定义
核心页面 = 项目库（home-view）+ 四步工作流（总板/画廊/详情）+ 设置账号区 + 导出对话框。
事实源：docs/THEME.md（墨航规范）、docs/REDESIGN-CHINA.md、docs/UI-REVIEW.md（旧账已清，仅剩 P2 VALIDATION_ERROR 文案一条）、e2e/visual-walkthrough.mjs 走查基建。

## 已知质量缺口（波次依据）
- C3/C2-FE 改版（账号区三态、SDK 生图链路 UI）验收时「截图证据链降级接受」——新表面从未经过完整视觉走查。
- 09-10 F6 走查之后无新鲜全量走查；其间 C 系列 21+ 文件改动入主干。

## 波次（滚动）
- **锁定区**：P1 走查评审（frontend·只读，Mock 模式零花费）→ 产出 docs/plans/2026-09-12-ui-walkthrough-findings.md 差距清单。
- **待细化区**：清单驱动拆分实现任务（预计 2-4 张 frontend 并行，按文件域切分避冲突）；细化触发条件 = P1 清单回报入账。清单条目严重度校准后逐张派发，随过随合并。
- 资产侧遗留（旧 FOLLOWUP F2/F3：high 总板/暗色总板补生成）：cms 化后 CLI 生图通道已变（需 cms key + llm:invoke），重启成本超出本波次，**不入本波锁定区**；待细化区挂起，触发条件 = 用户点名要补资产。

## 纪律
- 走查一律 Mock 模式，零 API 花费；真实生图不在本波次。
- 评审只读：允许新建 findings 文件与 e2e/screenshots/polish-*.png，禁止改既有文件与 git 状态。
- 实现任务派发时全局约束逐字下发：token 纪律（禁内联 hex/px，走 CSS 变量）、i18n 纪律（JSX 零硬编码文案）、THEME.md 为唯一视觉事实源。

## 进度账本
- 2026-09-12 07:32 计划建立（v1）。P1 任务书撰写中，即将派发。
- 2026-09-12 07:33 派发 P1（frontend·只读走查评审）→ 会话 session-a23dc677-0568-44e4-a095-c10523240c32，分支策略=无（只读不建分支），状态：运行中。Produces=findings 文件（docs/plans/2026-09-12-ui-walkthrough-findings.md）。等待回报。
- 2026-09-12 23:00（+08:00）**P1 验收通过并闭环**：验活核实 findings 180 行 / 40 张 polish-*.png / tmp 脚本已申报 / git 零修改既有文件；回报与盘上产物一致，清单七维自检+三态核对+盲区申报完整，发现 12 条（0C/3I/9M）校准合理。P2 遗留复验结论已由主会话入档 UI-REVIEW.md。
- 2026-09-12 23:00 **Ruling: THEME §5 对齐方向 = 修订 THEME 适配新 IA**（向导三段+工作台），不复活四步步条/悬停三件套——依据：IA 演进系已验收入主干的产品事实，THEME 服务于实现；错了的代价：若用户实际要四步步条回归，本波次 THEME+实现需二次返工（文档回滚+组件重写）。规格钉死（P2/P3 任务书逐字同款）：段标题三态=完成✓苍青/当前=缃色圆点+加粗/未来=muted；左菜单当前项=缃色圆点+加粗（替换 primary/10 底）。
- 2026-09-12 23:00 **Ruling: M3 采备选方案**（向导「✓ 锚点」提至 11px，不引 AnchorBadge）——依据：切断 P2×P3 在 card-actions.tsx 死代码迁移上的跨任务依赖，保三任务文件域两两不相交；错了的代价：徽标双轨暂存，留待细化区（触发=后续走查仍判刺眼）。
- 2026-09-12 23:00 **实现波次锁定区：P2/P3/P4 三张并行**（文件域两两不相交，i18n 键域预分配：P2=wizard.*+job.etaNote、P4=errors.*，禁重排 JSON）。P2=向导生成体验（I2 JobPanel+I1a 段标题缃色+M3 字号+M5 文案，域：create-wizard/job-panel 相关/i18n）；P3=THEME §5/§3 对齐（I1b left-menu 缃色+I3 死代码清理+AnchorBadge 迁移+M2 字阶条目+THEME 侧 M5，域：THEME.md/left-menu/card-actions）；P4=一致性小包（M1/M4/M6/M7/M8/M9，域：ui/input/switch-dialog/home-view/settings-dialog/detail-forms/favicon）。全部 frontend 型，worktree+短命分支，随过随合并。
- 2026-09-12 23:05 三任务书并行派发完成：P2→session-e7cc09e3（分支 feat/p2-wizard-jobpanel）、P3→session-a15d9552（分支 feat/p3-theme-align）、P4→session-b91af140（分支 feat/p4-consistency-pack），状态：均运行中。findings 清单+UI-REVIEW 复验行+P1 临时走查脚本（e2e/tmp-polish-*.mjs，验收期复跑要用）已入库，保证子会话 worktree 内可读事实源。等待三回报，随过随合并。
- 2026-09-12（+08:00 次日凌晨记）**P3 验收合并**：回报 DONE（3 笔提交 ef806f3 顶）。主会话独立核验——diff --stat 7 文件 +43/−99 与申报一致；THEME §5 钉死规格逐字入文（「缃色（accent）圆点」）；§3 补 30px 数据型大数字；card-actions 全库 grep 零残留；AnchorBadge 迁 anchor-badge.tsx 四处引用更新；3 条衍生改（§2 用途列/ghost 措辞/未点名死导出 AnchorIcon/PickIcon）均最小且未越域，裁定接受。ff 合并 5ccc5f4→ef806f3。**主干门禁复跑遇环境阻塞**：主 checkout pnpm build 两次超时（240s/180s，vite transforming 阶段挂），定性为环境负载（vscode fileWatcher 100% CPU + P2/P4 并行 worktree 构建争 IO），非代码问题（P3 worktree 内 build/test/cargo check 三绿 + diff 纯文档/死代码/import 路径性质）；后台重试中，绿后清 worktree+分支并推送。新事实入册：fresh worktree 缺 gitignored sidecar 二进制致 cargo 首跑失败——后续任务书模板加「worktree 前置：复制 src-tauri/binaries/ 或声明跳过 cargo 门禁」。
- 2026-09-12（+08:00 次日凌晨记）**P3 主干门禁复跑绿并闭环**：pnpm build exit 0（耗时 5m1s，正常 7s——环境负载定性再证）、pnpm test 81/81 exit 0（72.6s）。cargo check 免复跑（P3 零 Rust 改动，worktree 内同代码已绿）。异常观察：P3 worktree 回报后其已跟踪文件被外力批量删除（成排 D 状态，成因未明，P2/P4 会话无接触证据）；核实 3 笔提交全在 ef806f3 且已合并主干、无未合并工作后 `worktree remove --force` 清理，零源码损失。P3 生命周期终结（待推送确认）。
- 2026-09-12（+08:00 次日凌晨记）**P4 验收合并闭环**：回报 DONE（6 笔小步提交）。主会话独立核验——diff 7 文件 +103/−25 与申报一致；M7 hunk 逻辑正确（空转换→保留原文+报 slugFormat，空输入留 required 接管）；zh-CN.json slugFormat 键在位（L257）。三项申报裁决：①组件名称输入框同款静默清空一并修复——**接受**（同缺陷类同文件域最小衍生，循 P3 AnchorIcon 先例，可单独 revert）；②describeError 全库仅 settings-dialog 两处调用均已适配、契约无外溢——知悉；③lint:text 两条注释行既有红（tauri-api.ts:528/types.ts:118，非本包域）——历史欠账入待细化区微任务候选；④M6 日期为 zh-CN 数字式「2026/9/11」——locale 正确传递达标，汉字式口味留待细化区（触发=用户点名）。rebase main 6/6 无冲突（与 P3 的 home-view 交叠不同 hunk 自动合）→ ff 合并 a7d0ee8→ce0af53 → 主干门禁复跑绿（build exit 0 / test 82/82 净增 M8 用例）→ p4-*.png 5 张截图已拷主 checkout 留档 → worktree+分支已清 → 推送 ce0af53。**流程改进入册**：worktree 内验收截图须在清树前拷回主 checkout（P3 截图随树灭失，P4 起留档）。
- 2026-09-12（+08:00 次日凌晨记）**P2 验收合并闭环**：回报 DONE（4 笔提交）。主会话独立核验——diff 5 文件 +272/−20 与申报一致；JobPanel 零 prop 复用（`boardJob && <JobPanel job={boardJob}/>`，Produces 签名 ActiveJob 不变）、SectionTitle 三态 data-section-state+current=border-accent 环+bg-accent 内嵌点、锚点 text-[11px]、etaNote 双语改口径；wizard.* 键零新增（JobPanel 全量复用既有键，如实申报）。rebase 4/4 无冲突 → ff 合并 fcfe8b4→fb000e7 → 主干门禁 build exit 0 + **test 85/85**（净增 3 用例）→ p2-*.png 5 张留档 → worktree/分支已清 → 推送 fb000e7。盯防项 FLAKE-2 候选：P2 报一次 ELIFECYCLE 测试失败后两次复跑全绿（首编译负载抖动定性，未留失败日志——若主干再现同款，升级调查）。形制裁决：向导当前段=radio 形（accent 环+点）vs 左菜单=纯点，语义不同（步骤指示器 vs 列表当前项）均走 bg-accent token，裁定接受不返工，入待细化区（触发=走查判刺眼）。P2 提报的「THEME §5 文案 30~120 秒隐患」已被 P3 合并解决，无需动作。
- 2026-09-12（+08:00 次日凌晨记）**波次收官（计划 v1 锁定区全闭环）**：P1 走查（session-a23dc677）→ P2/P3/P4 实现（session-e7cc09e3/a15d9552/b91af140）四会话验收全过。收官汇总：主干门禁 build+test 85/85 全绿（origin/main=fb000e7）；Rust 门禁不变性由 diff 范围证明（三实现任务零 .rs/Cargo.lock 改动，主干 cargo test 228/0 为当日 07:22 新鲜证据）；行为证据抽查=四任务截图链 15 张留档 e2e/screenshots/（p2/p3/p4 各 5）+ P1 40 张 + 各 console 零报错记录。**归档名单**：a23dc677/e7cc09e3/a15d9552/b91af140（全部汇报已入账本；本环境无归档工具，留待 GUI 操作）。worktree/分支零残留。**待细化区存量盘点**：lint:text 2 行注释红（tauri-api.ts:528/types.ts:118）、日期汉字式口味、锚点徽标双轨统一、向导关闭后作业入口（需 workspace 表面）、缃色形制统一（radio vs 点）、资产侧 F2/F3 补生成（需 cms key 通道验证）、OAuth、C1b JWT。发现12条（3I+9M）全数处置：3I 修复合并、9M 中 6 条随 P4 修复、M3/M5 随 P2/P3 修复、M2 随 P3 修复。**波次目标达成：核心页面走查盲区补齐，全部 Important 清零，Minor 全清**。
- 2026-09-12 12:17（+08:00）**发布 0.2.0 启动**（用户指令「发布新版本 我来安装测试」）：CHANGELOG 0.2.0 节重写（Unreleased 四节吸收，Go server 上线→删除史压缩为净效果；新增「客户端全面对接 cms」与「界面打磨」两节）；版本三处 bump 0.1.0→0.2.0（package.json / tauri.conf.json / Cargo.toml workspace）+ Cargo.lock 三包同步 + README dmg 文件名；提交 54fb6a6 推送，tag v0.2.0 触发 CI（gate→build 三平台→release）。
- 2026-09-12 12:22 **失察热修：lint:text 是 CI gate 组成**。历史 main run 34662998150 failure 暴露：`check-hardcoded-text.mjs` 失败即 gate 挂、不进打包矩阵——正是 P2/P4 两度申报的「lint:text 主干预存红」（tauri-api.ts:528 / types.ts:118 注释含 CJK「偏差①」），我此前裁决「挂账待细化」属失察（未查其 CI 门禁链属性）。热修两行注释改英文（deviation #1），`pnpm lint:text` 绿，提交 bd11c4c；tag 删旧重打至 bd11c4c（无消费者窗口约 3 分钟，concurrency 组自动取消旧 run）。**教训入册**：子会话申报的「既有红/既有失败」必须先核其在不在 CI 门禁链上，在链即修不在链才可挂账。CI 监控后台进行，完成后汇报 Release 产物。
- 2026-09-12 12:38 **v0.2.0 发布成功**：run 34672843863 全绿（gate/build macos+ubuntu+windows/release 四 job success，墙钟约 16 分钟）；GitHub Release 上线 10 资产（dmg 8.06MB / app.zip / exe+msi / AppImage+deb / 裸 CLI×3 / SHA256SUMS）。Release URL：https://github.com/imeepos/ui-designer/releases/tag/v0.2.0。用户安装测试路径=macOS aarch64 dmg（未签名，首开需右键→打开放行 Gatekeeper）。用户首图冒烟（登录 cms→生成→扣点流水）恰可完成 P0 计费半边验证。发布编排闭环。
- 2026-09-12 12:44 **找活扫描（九源，发布收官后）**：①需求对账=PLAN/cms-integration/ui-polish 全闭环，无未对账需求；②Guesses 到期=无（data-scope 猜测已按条件作废）；③Known Issues=FLAKE-2 盯防中、stock 走查脚本 preview 冷启动超时无重试；④用户等待项=安装 v0.2.0 冒烟（准备链→R1 派发）、旧 Postgres 处置（准备链→决策备忘录落盘）、P0 计费冒烟（并入安装冒烟）；⑤质量债=lint:text 已修（待细化区条目随之关闭），其余触发条件制维持；⑥安全风险=零新增；⑦验收工具化=发布冒烟（本派）、e2e tmp 脚本转正决策；⑧待细化区=存量维持；⑨经验沉淀=波次教训已入账本三处，无新增。
- 2026-09-12 12:44 **R1 派发**（quality-gates：发布产物冒烟工具化）→ 会话 session-82d9ed34-9514-426e-a256-e68b2d4ba7bd，分支 feat/r1-release-smoke。Produces=scripts/release/smoke-release.sh（可复用）+ v0.2.0 实测健康报告（.agents/plans/2026-09-12-v020-release-smoke.md）。动机：用户安装测试前置——把「产物问题」vs「环境问题」的定位工具化。
- 2026-09-12 12:44 **Postgres 处置决策备忘录落盘**（.agents/plans/2026-09-12-postgres-retirement-memo.md）：三选项（A 维持/B 快照停用【建议】/C 彻底清除）+影响面+runbook，等人拍板的是处置本体，思考链已备齐。宿主技能更新（SKILL.md/agents/openai.yaml）随本提交入库。候选池新增：e2e 走查基建加固（stock 脚本重试+tmp 转正）；其余待细化区触发条件不变。
- 2026-09-12（+08:00 深夜记）**R1 验收合并闭环**：回报 DONE（单笔 08e0394，2 文件 +585 纯新增非构建链）。主会话核验 diff+报告——11/11 判定 PASS（SHA256 三方一致/dmg CRC verified/CFBundleShortVersionString=0.2.0/sidecar+裸 CLI --version 均 0.2.0），bash -n 绿、lint:text 绿。ff 合并 62b9454→08e0394，worktree/分支已清，推送完成。**产物健康结论：用户报障优先按环境问题排查**。三个修正任务书假设的新事实入册：①包内 sidecar 名 `rudder`（无 triple 后缀）、主二进制名 `rudder-app`；②签名为 adhoc+linker-signed（非纯 unsigned），未公证 Gatekeeper 仍拦；③SHA256SUMS.txt 路径带 artifacts 前缀不能直接 shasum -c。新版本发布后复跑 `smoke-release.sh v<tag>`；候选池新增：shellcheck 补门禁、Rudder-app.zip 装法实测（低风险）。
